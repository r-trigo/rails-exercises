<%= form_for(pkm) do |f| %>
  <% if pkm.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(pkm.errors.count, "error") %> prohibited this pkm from being saved:</h2>

      <ul>
      <% pkm.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :comp_id %>
    <%= f.text_field :comp_id %>
  </div>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :region_name %>
    <%= f.text_field :region_name, data: { autocomplete_source: regions_path } %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>